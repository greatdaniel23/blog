---
interface Props {
	date: Date;
	locale?: 'id' | 'en';
}

const { date, locale = 'id' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale === 'en' ? 'en-US' : 'id-ID', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
